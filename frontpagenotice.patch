diff --git a/app/Http/Controllers/Client/DashboardController.php b/app/Http/Controllers/Client/DashboardController.php
index 9736d25..e469d15 100644
--- a/app/Http/Controllers/Client/DashboardController.php
+++ b/app/Http/Controllers/Client/DashboardController.php
@@ -56,6 +56,9 @@ public function index(Request $request)
             ->take(5)
             ->get();
 
+        // Notices flagged as an out-of-office banner across the top of the page.
+        $frontPageNotices = Noticeboard::frontPage()->get();
+
         $portalStatus = PortalStatus::find(1);
 
         $todayHours = OpeningHour::where('day_of_week', now()->dayOfWeek)->first();
@@ -76,6 +79,7 @@ public function index(Request $request)
             'spendLabels',
             'recentFileRequests',
             'notices',
+            'frontPageNotices',
             'portalStatus',
             'todayHours',
             'totalSpent',
diff --git a/app/Http/Controllers/Owner/NoticeboardController.php b/app/Http/Controllers/Owner/NoticeboardController.php
index a69cf7b..298a06f 100644
--- a/app/Http/Controllers/Owner/NoticeboardController.php
+++ b/app/Http/Controllers/Owner/NoticeboardController.php
@@ -21,6 +21,9 @@ public function store(StoreNoticeboardRequest $request): RedirectResponse
     {
         $data = $request->validated();
         $data['created_by_user_id'] = auth()->id();
+        // Unchecked checkboxes aren't sent — resolve them explicitly.
+        $data['is_active'] = $request->boolean('is_active');
+        $data['show_on_front_page'] = $request->boolean('show_on_front_page');
 
         Noticeboard::create($data);
 
@@ -29,7 +32,13 @@ public function store(StoreNoticeboardRequest $request): RedirectResponse
 
     public function update(StoreNoticeboardRequest $request, Noticeboard $noticeboard): RedirectResponse
     {
-        $noticeboard->update($request->validated());
+        $data = $request->validated();
+        // Unchecked checkboxes aren't sent — resolve them explicitly so toggling
+        // Active / Display on Front Page off actually persists.
+        $data['is_active'] = $request->boolean('is_active');
+        $data['show_on_front_page'] = $request->boolean('show_on_front_page');
+
+        $noticeboard->update($data);
 
         return back()->with('success', 'Notice updated.');
     }
diff --git a/app/Http/Requests/Owner/StoreNoticeboardRequest.php b/app/Http/Requests/Owner/StoreNoticeboardRequest.php
index 43a3831..c2f2b57 100644
--- a/app/Http/Requests/Owner/StoreNoticeboardRequest.php
+++ b/app/Http/Requests/Owner/StoreNoticeboardRequest.php
@@ -22,6 +22,7 @@ public function rules(): array
             'show_from' => ['nullable', 'date'],
             'show_until' => ['nullable', 'date', 'after_or_equal:show_from'],
             'is_active' => ['boolean'],
+            'show_on_front_page' => ['boolean'],
         ];
     }
 }
diff --git a/app/Models/Noticeboard.php b/app/Models/Noticeboard.php
index 4e1de2c..76ef5cf 100644
--- a/app/Models/Noticeboard.php
+++ b/app/Models/Noticeboard.php
@@ -19,6 +19,7 @@ class Noticeboard extends Model
         'show_from',
         'show_until',
         'is_active',
+        'show_on_front_page',
     ];
 
     protected function casts(): array
@@ -28,6 +29,7 @@ protected function casts(): array
             'show_from' => 'date',
             'show_until' => 'date',
             'is_active' => 'boolean',
+            'show_on_front_page' => 'boolean',
         ];
     }
 
@@ -49,4 +51,14 @@ public function scopeActive($query)
             })
             ->orderByRaw("CASE priority WHEN 'high' THEN 1 WHEN 'normal' THEN 2 WHEN 'low' THEN 3 ELSE 4 END");
     }
+
+    /**
+     * Currently-visible notices flagged to pin as a prominent "out of office"
+     * banner on the dealer dashboard. Builds on scopeActive() so the same
+     * show_from/show_until scheduling applies.
+     */
+    public function scopeFrontPage($query)
+    {
+        return $query->active()->where('show_on_front_page', true);
+    }
 }
diff --git a/database/migrations/tenant/2026_07_26_000001_add_show_on_front_page_to_noticeboards.php b/database/migrations/tenant/2026_07_26_000001_add_show_on_front_page_to_noticeboards.php
new file mode 100644
index 0000000..68117b6
--- /dev/null
+++ b/database/migrations/tenant/2026_07_26_000001_add_show_on_front_page_to_noticeboards.php
@@ -0,0 +1,29 @@
+<?php
+
+use Illuminate\Database\Migrations\Migration;
+use Illuminate\Database\Schema\Blueprint;
+use Illuminate\Support\Facades\Schema;
+
+return new class extends Migration
+{
+    public function up(): void
+    {
+        Schema::table('noticeboards', function (Blueprint $table) {
+            if (! Schema::hasColumn('noticeboards', 'show_on_front_page')) {
+                // When true, the notice is pinned as a prominent "out of office"
+                // banner across the top of the dealer dashboard (e.g. extended
+                // holiday closure), in addition to the normal notices list.
+                $table->boolean('show_on_front_page')->default(false)->after('is_active');
+            }
+        });
+    }
+
+    public function down(): void
+    {
+        Schema::table('noticeboards', function (Blueprint $table) {
+            if (Schema::hasColumn('noticeboards', 'show_on_front_page')) {
+                $table->dropColumn('show_on_front_page');
+            }
+        });
+    }
+};
diff --git a/resources/views/client/dashboard.blade.php b/resources/views/client/dashboard.blade.php
index dd98c4a..5a0423f 100644
--- a/resources/views/client/dashboard.blade.php
+++ b/resources/views/client/dashboard.blade.php
@@ -1,5 +1,23 @@
 <x-layouts.client>
 
+    <!-- Out-of-office / front-page notices -->
+    @foreach ($frontPageNotices as $notice)
+        <div class="mb-6 rounded-xl border border-amber-500/40 bg-amber-500/10 px-5 py-4 flex items-start gap-4">
+            <svg class="w-6 h-6 text-amber-400 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
+                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01M10.29 3.86L1.82 18a2 2 0 001.71 3h16.94a2 2 0 001.71-3L13.71 3.86a2 2 0 00-3.42 0z"/>
+            </svg>
+            <div class="flex-1">
+                <p class="text-sm font-semibold text-amber-300">{{ $notice->title }}</p>
+                @if ($notice->body)
+                    <p class="text-sm text-amber-100/80 mt-1 whitespace-pre-line">{{ $notice->body }}</p>
+                @endif
+                @if ($notice->show_until)
+                    <p class="text-xs text-amber-200/60 mt-2">Until {{ $notice->show_until->format('j M Y') }}</p>
+                @endif
+            </div>
+        </div>
+    @endforeach
+
     <!-- Welcome banner -->
     <div class="flex flex-wrap items-start justify-between gap-4 mb-6">
         <div class="flex-1">
diff --git a/resources/views/owner/noticeboards/_form.blade.php b/resources/views/owner/noticeboards/_form.blade.php
index face011..9a7eca7 100644
--- a/resources/views/owner/noticeboards/_form.blade.php
+++ b/resources/views/owner/noticeboards/_form.blade.php
@@ -36,12 +36,19 @@ class="mt-1 block w-full rounded-md border-gray-300 dark:border-[#2a2a2a] dark:b
     </div>
 </div>
 
-<div class="flex items-center gap-6">
+<div class="flex flex-wrap items-center gap-6">
     <label class="inline-flex items-center gap-2 text-sm text-gray-700 dark:text-gray-300">
         <input type="checkbox" name="is_active" value="1" @checked(old('is_active', $noticeboard?->is_active ?? true))>
         Active
     </label>
+    <label class="inline-flex items-center gap-2 text-sm text-gray-700 dark:text-gray-300">
+        <input type="checkbox" name="show_on_front_page" value="1" @checked(old('show_on_front_page', $noticeboard?->show_on_front_page ?? false))>
+        Display on Front Page
+    </label>
 </div>
+<p class="-mt-2 text-xs text-gray-500 dark:text-gray-400">
+    Pins this notice as a prominent banner across the top of the dealer dashboard — use it for an out-of-office message (e.g. an extended holiday closure).
+</p>
 
 <div class="flex justify-end gap-3 pt-2">
     <button type="button" x-on:click="open = false" class="px-4 py-2 rounded-md text-sm text-gray-600 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-700">
diff --git a/resources/views/owner/noticeboards/index.blade.php b/resources/views/owner/noticeboards/index.blade.php
index 6abbf50..fce3079 100644
--- a/resources/views/owner/noticeboards/index.blade.php
+++ b/resources/views/owner/noticeboards/index.blade.php
@@ -15,12 +15,15 @@ class="px-4 py-2 rounded-md bg-brand text-white text-sm font-medium hover:bg-[#c
                 </td>
                 <td class="px-4 py-3 text-sm text-gray-700 dark:text-gray-300">{{ $noticeboard->show_from?->format('d/m/Y') ?? '-' }}</td>
                 <td class="px-4 py-3 text-sm text-gray-700 dark:text-gray-300">{{ $noticeboard->show_until?->format('d/m/Y') ?? '-' }}</td>
-                <td class="px-4 py-3 text-sm">
+                <td class="px-4 py-3 text-sm space-x-1">
                     @if ($noticeboard->is_active)
                         <x-status-badge status="Active" colour="bg-green-100 text-green-800" />
                     @else
                         <x-status-badge status="Inactive" colour="bg-gray-100 text-gray-800" />
                     @endif
+                    @if ($noticeboard->show_on_front_page)
+                        <x-status-badge status="Front Page" colour="bg-amber-100 text-amber-800" />
+                    @endif
                 </td>
                 <td class="px-4 py-3 text-sm text-gray-700 dark:text-gray-300">{{ $noticeboard->created_at->format('d/m/Y') }}</td>
                 <td class="px-4 py-3 text-sm text-right space-x-3">
